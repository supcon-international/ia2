# Force ts-rs to emit generated TS bindings into apps/web/src/types/generated/.
# `relative = true` resolves against the parent dir of this config file (workspace root).
[env]
TS_RS_EXPORT_DIR = { value = "apps/web/src/types/generated", relative = true }
