# Registry manifest — served by GET /api/library. Bump `version` when
# any block changes; projects record the imported version in their
# project.toml [libraries] table, which is how the IDE knows to offer
# an update.
name = "process-control"
version = "0.1.0"
description = "Engineering-essential process-control blocks: signal conditioning, PID loops, alarms, device control (27 FBs)"
